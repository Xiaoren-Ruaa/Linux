name: Build All Four Core Libs (Full Fix v9)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout util-linux
        uses: actions/checkout@v4
        with:
          repository: util-linux/util-linux
          fetch-depth: 1

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gettext bison flex autopoint build-essential pkg-config libtool autoconf automake

      - name: Pre-build Source Surgery
        run: |
          # 1. 彻底清空 monitor_fanotify.c 的内容，防止其包含不存在的 sys/fanotify.h (解决图 3459)
          echo "/* Cleaned for Android */" > libmount/src/monitor_fanotify.c
          echo "int mnt_monitor_enable_fanotify(void *mn, int enable) { return -1; }" >> libmount/src/monitor_fanotify.c
          echo "int mnt_monitor_get_fanotify_fd(void *mn) { return -1; }" >> libmount/src/monitor_fanotify.c

          # 2. 移除 Makefile 模板中对 fanotify 的依赖，确保不会被再次编译
          sed -i 's/monitor_fanotify.c//g' libmount/src/Makemodule.am

          # 3. 修复 versionsort 和 shells 相关符号 (解决图 3447, 3454, 3456)
          sed -i 's/versionsort/alphasort/g' libmount/src/tab_parse.c
          cat <<EOF >> lib/strutils.c
          #include <string.h>
          int strverscmp(const char *s1, const char *s2) { return strcmp(s1, s2); }
          void setusershell(void) {}
          void endusershell(void) {}
          char *getusershell(void) { return (char *)0; }
          EOF

      - name: Build Core Libraries
        run: |
          ./autogen.sh
          
          export TOOLCHAIN=$ANDROID_NDK_LATEST_HOME/toolchains/llvm/prebuilt/linux-x86_64
          export TARGET=aarch64-linux-android33
          export CC=$TOOLCHAIN/bin/$TARGET-clang
          export STRIP=$TOOLCHAIN/bin/llvm-strip

          export CFLAGS="-Wno-implicit-function-declaration -Wno-int-conversion"
          export LDFLAGS="-Wl,-rpath=/system/lib64 -Wl,--dynamic-linker=/system/bin/linker64 -Wl,--allow-shlib-undefined"

          ./configure --host=aarch64-linux-android \
                      --prefix=/system \
                      --enable-shared \
                      --disable-static \
                      --disable-all-programs \
                      --enable-libuuid \
                      --enable-libblkid \
                      --enable-libmount \
                      --enable-libsmartcols \
                      --disable-bash-completion \
                      --without-python \
                      --without-udev \
                      --without-ncurses \
                      --disable-libmount-mountfd-support \
                      --disable-monitor-fanotify \
                      --disable-monitor-utab

          make -j$(nproc)

          # --- 提取与重构产物 ---
          mkdir -p output/lib64
          for lib in uuid blkid mount smartcols; do
            cp .libs/lib${lib}.so output/lib64/
            # 建立 Android 必需的 .so.1 软链接
            ln -sf lib${lib}.so output/lib64/lib${lib}.so.1
          done
          
          cp .libs/lsblk output/
          find output/lib64/ -type f -name "*.so" -exec $STRIP --strip-unneeded {} +
          $STRIP output/lsblk

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: util-linux-libs-v9-surgery
          path: output/
